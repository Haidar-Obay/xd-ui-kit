import React from "react";
import { TextArea } from "../components/TextArea";

export default {
  title: 'Form_Element/TextArea',
  component: TextArea,
};

export const Default = () => <TextArea />;
